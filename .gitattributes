# Windows と macOS で working copy を一致させるため、
# スクリプト及びスクリプトから読まれるテキストは LF で統一する。
# CRLF 化されるとシェバングが "bash\r" になったり、
# bash 経由で読んだテキスト末尾に \r が混入して壊れるため。
*.sh  text eol=lf
*.ps1 text eol=lf
*.txt text eol=lf
